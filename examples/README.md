# Example CSV files

Curated sample data for manual smoke tests and documentation.

The app loads these at runtime from [`public/examples/`](../public/examples/) via **Load sample files** on the home page. Keep both folders in sync when updating samples.
